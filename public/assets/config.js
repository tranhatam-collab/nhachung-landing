/* public/assets/config.js
   Landing config (no secrets).
   This is intentionally static, safe to cache.
*/
(function () {
  window.NHACHUNG = {
    brand: "Nhà Chung",
    langDefault: "vi",
    domains: {
      root: "https://nhachung.org",
      app: "https://app.nhachung.org",
      api: "https://api.nhachung.org"
    }
  };
})();
